import React from 'react';
import { View } from 'react-native';

// This is a dummy file to satisfy Expo Router for the "create" tab.
// The actual navigation is intercepted in _layout.tsx to open the modal.
export default function CreateTab() {
  return <View />;
}
